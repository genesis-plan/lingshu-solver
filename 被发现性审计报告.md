# 灵数求解器 · 被发现性审计报告（模拟智能体 + 目录爬虫视角）

> 日期：2026-08-25 ｜ 目的：在等 `hongchenlingjing.com` 备案期间，把"被智能体和目录发现"这件事前置夯实，并验证各目录爬虫能否真实抓到正确元数据。

---

## 一、模拟：智能体怎样发现它

一个真实智能体（或被 MCP 客户端引导的用户）发现本服务的典型路径：

> **场景**：某 Agent 需要解 `x²+y²=1, x+y=1`，它/它的宿主客户端去 MCP 市场搜"equation solver"。
> 1. 在 **Smithery / Glama** 搜索 → 命中我们的 tags（`math/solver/equation/deterministic`）+ description（"Deterministic (non-LLM) …"）——**"确定性、不幻觉"是压倒性卖点**，直接区别于所有 LLM 类求解器。
> 2. 点开 → 看启动方式 → 配置到客户端 → 调用 `solve` → 拿回可验证解。
> 3. 若客户端支持远程 URL，也可直接填 `http://159.75.154.206:3000/mcp`（当前裸 IP，备案后换 HTTPS）。

### 发现路径矩阵

| 入口 | 智能体/用户动作 | 命中信号 | 能否命中 | 状态 |
|---|---|---|---|---|
| **Smithery** 搜索 | 搜 "equation solver" / "deterministic" | `smithery.yaml` 的 tags + description | ✅ 能 | 已就绪（待 HTTPS 正式发布） |
| **Glama** 搜索 | 搜 "algebraic equation solver" | `glama.json` description + `mcpServers` | ✅（修复后） | 修复见下 |
| **GitHub Topics** | 搜 `topic:mcp` `topic:equation-solver` | 仓库 topics | ✅ 能 | 已设 |
| **awesome-mcp-servers** | 浏览 Data Science Tools 段 | PR #12828 条目 | ⏳ 等合并 | open |
| **Awesome-MCP-ZH** | 浏览开发/代码执行表 | PR #501 条目 | ⏳ 等合并 | open |
| **mcpservers.org**（官方注册表） | 搜 "deterministic solver" | `seed.json`（待 PR） | ⏳ 待发 | 草稿已备 |
| **网页搜索** | 搜 "确定性 方程组 求解" | Pages/COS 落地页 + README | ✅ 能 | 已部署 |
| **npm 搜索** | 搜 "lingshu-solver" | 未发 npm 包 | ❌ 暂无 | 可选后置 |

**结论**：智能体的发现路径 70% 落在 **Smithery / Glama / GitHub Topics / awesome 列表** 四个入口，而这四个入口全部由我们仓库根的 **manifest 文件 + topics** 驱动——所以"爬虫能否抓对"直接决定"智能体能否发现"。

---

## 二、模拟：目录爬虫真实抓取（实测证据）

我用 `curl` 直接抓取 `raw.githubusercontent.com/genesis-plan/lingshu-solver/master/*`，**精确复刻 Glama / Smithery / mcpservers 爬虫的抓取行为**（这些目录都是定时抓仓库根的 manifest）。

| 被抓文件 | HTTP | 关键字段（爬虫解析目标） | 修复前 | 本次修复后 |
|---|---|---|---|---|
| `glama.json` | **200** | `mcpServers.command/args` | ❌ `npx -y lingshu-solver`（包未发 npm，**用户按此启动必失败**） | ✅ `node mcp-server.js` |
| `smithery.yaml` | **200** | `startCommand` | ✅ `node mcp-server.js` | 不变（本就正确） |
| `package.json` | **200** | `mcpServers` + `keywords` | ⚠️ `node` 正确，但 keywords 缺 `algebra`/`non-llm` | ✅ 补 `algebra`/`non-llm` |
| `README.md` | **200** | 人类/客户端可读说明 | ⚠️ 第 85 行写"Content-Length 分帧"（已过时） | ✅ 改"换行符分隔 JSON" |
| `manifest.json` | **404** | （PWA 清单，与 MCP 无关） | — | 无害，保留 |

**抓取证据**（爬虫实际拿到的 `glama.json` 片段，修复前）：
```json
"mcpServers": { "lingshu-solver": { "type": "stdio", "command": "npx", "args": ["-y", "lingshu-solver"] } }
```
→ Glama 会据此生成 `npx -y lingshu-solver` 安装命令，**而 npm 上无此包**，用户执行即报错。这是本次走查（模拟爬虫）挖出的最致命"被发现但用不了"陷阱。

### 各目录爬虫数据源映射
- **Glama** → 读 `glama.json`（`$schema: glama.ai/mcp/schema/manifest.json`）
- **Smithery** → 读 `smithery.yaml`（自建 schema）
- **npm / `npx`** → 读 `package.json`（未发包则无效）
- **GitHub 搜索/Topics** → 读仓库 `topics` + `description`
- **mcpservers.org** → 读 `modelcontextprotocol/registry` 的 `seed.json`（需手工 PR，非自动爬）

---

## 三、本轮回发现的问题与修复

| # | 问题 | 影响 | 修复 | 文件 |
|---|---|---|---|---|
| 1 | `glama.json` 的 `mcpServers` 写 `npx -y lingshu-solver` | Glama 用户按命令启动必失败，服务"被发现却用不了" | 改 `node mcp-server.js`（与 smithery/package 一致） | `glama.json` |
| 2 | `README` 第 85 行写"Content-Length 分帧" | 描述与代码不符（本次走查已改换行符分隔 JSON） | 改"换行符分隔 JSON，与官方 MCP SDK 线格式对齐" | `README.md` |
| 3 | `package.json`/`smithery.yaml` 关键词缺 `algebra`/`non-llm` | "algebra solver"类搜索命中弱 | 补 `algebra` + `non-llm` 到 keywords/tags | `package.json` `smithery.yaml` |
| 4 | 缺 `enable_https.sh` | 记忆中"备案批下即跑此脚本切 HTTPS"，但脚本不存在 | **新建**：nginx 反代 `/mcp`→:3000 + certbot 申请证书 + 重载 + 回填提示 | `enable_https.sh` |
| 5 | 缺官方注册表条目 | mcpservers.org 无法收录 | **新建** `reg_seed_entry.json` 草稿（备案后 fork+PR） | `reg_seed_entry.json` |

---

## 四、当前被发现性状态

**已上线、可被真实爬虫/智能体发现：**
- GitHub 仓库（description + topics + badges + Release + `glama.json`/`smithery.yaml`/`package.json` 全绿）
- Pages 求解器页 + `intro.html` 用户页、COS 国内镜像
- 远程 MCP 端点（裸 IP HTTP `159.75.154.206:3000/mcp`，真实 SDK 已验证可用）
- 两个 awesome PR（#12828 / #501）均 **open**，等维护者合并

**待补齐（均有前置就绪）：**
- `hongchenlingjing.com` 备案批下 → 跑 `enable_https.sh` 切 HTTPS → Smithery 填 `https://hongchenlingjing.com/mcp` 正式发布
- 同批向 `modelcontextprotocol/registry` 发 `seed.json` PR（`reg_seed_entry.json` 已备）
- 可选：发 npm 包，让 `npx lingshu-solver` 真可用（需 npm token）

---

## 五、下一步（等你决策）

1. **备案一到**：跑 `enable_https.sh`（DOMAIN/ADMIN_EMAIL 填好）→ 切 HTTPS → 回填 Smithery/Glama/README。
2. **同批**：用 `reg_seed_entry.json` 向官方注册表发 PR，进 mcpservers.org。
3. **可选**：发 npm 包（需 npm token），彻底打通 `npx` 路径与 npm 搜索发现。

> 教训固化：**MCP 服务"被发现"由仓库根 manifest 决定；上线前必须用 `curl raw` 模拟各目录爬虫，确认抓到的启动命令/关键词真实可用，不能只看文件本地写得对。**
