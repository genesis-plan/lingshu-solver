# 灵数求解器 · 产品定位话术（多专家打磨版 v1）

> 打磨视角：战略 / 定位 / 文案 / 销售 / 创业实战
> 核心结论：上一版对外材料是「工程师视角」（讲内部怎么算、把"6变量/10方程"当卖点）。用户不关心这些。
> **真正的差异化只有两个字——确定性。** 同样的题永远得到同样的答案、没有大模型的随机与幻觉、每个解都能回代验证。这是当前所有"AI 数学工具"都给不了的，最锋利也最诚实。

---

## 0. 一句话结论

- **对外只讲用户价值，不讲内部算法。** 用户问的是"这个答案我能信吗"，不是"你用的什么分支定界"。
- **刀刃 = 确定性（无随机 / 无幻觉 / 可验证）。** 这是相对大模型数学工具最锋利的差异，也是相对传统商用求解器最轻量的差异。
- **"10方程限制"是旧叙事，已无此封顶，对外一律删除。** 真实约束见下。

## 1. 真实能力边界（对外只说这些，绝不编造）

| 项目 | 当前真实状态 |
|---|---|
| 未知数上限 | 最多 **6 个** |
| 方程个数 | 须 **≥ 未知数个数**；**无"最多10条"封顶** |
| 答案范围 | **±100万**（约 ±1e6） |
| 精度 | 最多 6 位小数，验证误差 < 百万分之一 |
| 算法性质 | **确定性**（区间分支定界），非大模型、无随机 |
| 诚实口径 | 已验证解数学保真；全局穷尽**尽力而为**（不承诺"保证不漏解"） |
| 交付形态 | 纯网页（打开即用）/ MCP 工具（给 AI 智能体调用） |

> 红线：整数强制约束、必不等于约束目前**做不了**（UI 已注明），话术不得暗示能做。

## 2. 核心价值主张：确定性（The Wedge）

**对比表（大模型数学工具 vs 灵数求解器）**

| 维度 | 大模型数学工具（ChatGPT / Claude 等） | 灵数求解器 |
|---|---|---|
| 同题答案 | 每次可能不同（温度 / 随机） | **完全一致（确定性）** |
| 会幻觉吗 | 会，给出"看似合理"的错解 | **不会**，算法穷举 + 验证 |
| 可验证 | 自然语言解释，难核验 | **每个解可回代验证** |
| 适合自动化 | 不可靠，Agent 不敢信 | **Agent 可稳定调用（MCP）** |
| 部署 | 需联网调 API | 网页 / 本地 / 自托管 |

**三个确定性支柱**
1. **同输入同输出** —— 可复现、可审计
2. **无幻觉** —— 不编答案，只有"算到 / 没算到"
3. **可验证** —— 每个解标注认证状态（proven / likely / candidate）+ 残差

## 3. 多专家视角打磨

### 战略专家：差异化与护城河
- 不与 Wolfram / Matlab 拼"算得最准最大"（那是烧钱军备竞赛）；
- 也不与大模型拼"能聊天"（那是别人的主场）；
- **切"确定性数值工具"这个缝隙品类**：大模型天生做不到（随机），传统商用太重（要授权）。
- 护城河 = **确定性 + 可验证 + 能当 Agent 工具（MCP）**，三者叠加不易抄。

### 定位专家：品类与心智占位
- 心智词：**"不幻觉的数学工具" / "给 Agent 的确定性计算器"**
- 占位目标：当用户想"算个准的数、不想被 AI 编"时，第一个想到灵数求解器。
- 避开红海"又一个方程组求解器"，占位蓝海"确定性"赛道。

### 文案专家：口号 + 落地页首屏（候选，供市场选）
- A. 「同样的题，永远同一个答案。」
- B. 「不靠猜，靠算。不幻觉的方程组求解器。」
- C. 「给 AI 一个不会胡说八道的数学大脑。」
- D. 「确定性数值求解 · 网页打开即用 · 也能喂给 Agent」

落地页首屏（用户视角，**不提内部算法**）：
> 你是不是也受够了 AI 算数"时灵时不灵"？
> 灵数求解器是**确定性**的：同样的方程组，每次都给你**完全相同、可验证**的实数解。
> 不用装软件，打开网页就能用；也能当 MCP 工具，直接接进你的 AI 智能体。

### 销售专家：痛点 → 方案 → 证据
- **痛点**：财务建模、工程约束、Agent 推理要解方程组，但大模型"会编"、Excel 解不了非线性、商用软件太贵。
- **方案**：灵数求解器——确定、可验证、免费、打开即用 / 可接入 Agent。
- **证据**：`x²+y²=25` 且 `x+y=7` → 稳定返回 `(4,3)(3,4)`，均 `proven`，残差 < 1e-6（任何人可当场复现）。
- **收口**：免费先用，MCP 接入零成本，企业可自托管。

### 创业实战专家：GTM + 冷启动 + 变现
- **冷启动**：不买量，靠"确定性"做内容钩子（知乎 / 掘金 / HN 上"AI 算数不靠谱"话题天然有流量）。
- **先打透一类人**：**AI Agent 开发者**（MCP 场景，确定性是刚需，且会自发传播）。
- **变现路径（被动、不主动推销，符合既定策略）**：
  1. 个人 / 会计免费轻量使用（口碑 + 流量）
  2. 企业自托管 / 私有化部署（收费，卖点 = 确定性 + 数据不出域）
  3. MCP 被 Agent 生态广泛接入后，做"高并发云端确定性求解"按量付费
  4. 软著 + 发明专利（V4.1 Epsilon）形成资产壁垒

## 4. 分人群话术（复制即用）

**给个人 / 学生 / 会计**
> 一个不胡说的方程组计算器。非线性、带约束都能解，打开网页就用，答案每次都一样、还能回代验算。免费。

**给 AI Agent 开发者（最关键）**
> 你的 Agent 需要算数，但大模型天生会编、会随机。灵数求解器是**确定性** MCP 工具：同样的约束，每次返回完全一致、可验证的实数解。给 Agent 一个"不会胡说八道"的数学后端，推理才 reproducible。
> 接入：clone 后 `node mcp-server.js`，客户端配一个 URL / command 即可。

**给企业 / 私有化**
> 确定性数值求解，数据不出域、可自托管、已验证解数学保真。适合把"可靠数学"嵌入内部系统或 Agent 管线。

## 5. 对外口径红线（诚实，不可越）

- ✅ 说：确定性、可验证、无幻觉、同输入同输出、已验证解数学保真、全局穷尽尽力而为
- ✅ 说：最多 6 变量、方程 ≥ 变量数、范围 ±100万、6 位小数
- ❌ 不说："保证不漏解""100% 全覆盖""绝对精确"
- ❌ **不再提"10方程限制"**（旧叙事，当前无此封顶）
- ❌ 不编功能：整数强制 / 必不等于约束目前做不了

## 6. 下一步（可立即执行）

- [ ] 用本话术重写 `promotion-kit.md` 与 `smithery-listing.md`（去"10方程"卖点，改"确定性"）
- [ ] 落地页首屏文案强化"确定性"（副标题已是，正文首屏可补）
- [ ] 给 awesome-mcp-servers 发 PR 时统一用确定性话术
- [ ] 备齐后：企业自托管 / 云端按量 两条收费线立项
