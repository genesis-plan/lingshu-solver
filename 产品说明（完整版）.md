# 灵数求解器 · 产品说明（完整版 / 对外统一口径）

> 用途：人看的"完整规格 + FAQ"，也是所有对外文案（落地页、目录、Smithery、README）的**唯一事实来源**。
> 核心定位：**确定性**数值方程组求解工具——同样的题永远得到同样的答案，没有大模型的随机与幻觉，每个解都能回代验证。
> 原则：诚实优先，最大化让别人选择、用得方便、反馈方便。

---

## 0. 它是什么 / 不是什么

| | 说明 |
|---|---|
| **是** | 确定性（非大模型）的实数方程组**数值**求解引擎。代数方程 + 常见超越函数（sin/cos/tan/log/exp/sqrt/abs）均可。 |
| **不是** | 符号 CAS（不输出解析推导/闭式证明）、微分方程初值求解器、整数规划/丢番图约束求解器。 |

> 一句话给人：**"同样的题，永远同一个答案。不靠猜，靠算。"**

---

## 1. 数值范围限制

- 每个变量的**答案**必须在 **±100万（≈ ±1e6）** 之间；超出此范围可能找不到解。
- 可通过 `domain` 显式收窄搜索域（如 `{"x":[-30,30]}`），对快增长函数（exp/sinh）或"部分收敛"场景**强烈建议显式给定**，否则默认 ±1e6 可能剪枝失效。

## 2. 变量数量范围

- **最多 6 个未知数**。超出返回 `invalid_input` 错误。
- 变量名可不填，由方程文本自动识别（按出现顺序）；也可显式传入 `variables`。

## 3. 方程数量与形式

- 方程**数量须 ≥ 未知数个数**（否则约束不足，无法定解）。
- MCP 服务端护栏：**最多 64 条方程**（UI 无此硬封顶，但服务端防滥用设 64 上限）；单次请求方程文本总长 **≤ 100KB**。
- 形式：含 `=` 的方程字符串数组，如 `["x^2 + y^2 = 25", "x + y = 7"]`；支持 in-text 域约束 `"x ∈ [-30,30]"`。
- 支持运算：`+ - * / ^`、函数 `sqrt log sin cos tan exp abs`，以及微分/积分/极限等语法糖（数值化后求解）。

## 4. 输出小数精度

- **固定 6 位小数**（产品规格"6位小数有限网格"），不提供位数切换，与网页版完全一致。
- 残差容差三档（按模式）：**Balanced 1e-6 / Precise 1e-9 / Fast 1e-3**（`fastMode` 切到 Fast）。

## 5. 计算结果的性质（重点）

求解引擎是**确定性**的：相同输入永远返回**完全相同**的结果，无随机、无温度——可缓存、可复现、可审计。

**结果三态（`resultType`）**

| 状态 | 含义 |
|---|---|
| `empty` | 经 sound 算子**严格证明无实数解**（如平行矛盾、结构恒正/恒负）。`provenEmpty=true`。 |
| `finite` | 找到有限个已验证实数解。 |
| `infinite` | 无限多个连续解（参数化）；仅输出**距原点最近的推荐解**（`recommended`）。 |

**每个解对象（`solutions[]`）字段**

| 字段 | 含义 |
|---|---|
| `values[]` | 数值解（按 variables 顺序） |
| `tier` | 认证层级：`proven`（Krawczyk 已认证）/ `likely` / `candidate` |
| `residual` | 回代残差，越小越准 |

**顶层字段**：`certified`（是否全部 proven）、`solutionCount`、`recommended`（范数最小/距原点最近的解）、`truncated`、`warnings[]`、`diagnostics`。

**`truncated` 的含义（诚实关键）**
- `truncated=true` 表示：**预算内未完成全局分支判定，未证明已穷尽**。
- **不等于一定漏解**——绝大多数情况下全部真解已找到。
- 仅在极端病态（雅可比高度奇异、解簇极近）下可能遗漏个别解；此时可**缩 `domain` 或提高 `budget` 重试**。

## 6. 错误情况与提示

| 错误 `type` | 触发 | 提示 |
|---|---|---|
| `invalid_input` | 方程非非空字符串数组 / 单条非字符串 / 方程数 >64 / 文本 >100KB / 变量 >6 | 返回具体超限信息，修正后重试 |
| `internal_error` | 内部异常 | 结构化返回错误类型，**绝不泄露内部堆栈** |
| `unknown_tool` | 调用了不存在的工具 | 检查工具名 |

> 遇卡点、认为结果有误、或有建议 → 调用 `give_feedback`（内容仅落本地 `feedback.log`，**不外传**）。

## 7. 对别人的作用（价值）

| 用户 | 作用 |
|---|---|
| 个人 / 学生 / 会计 | 一个不胡说的方程组计算器：非线性、带约束都能解，打开网页即用，答案每次一致、可回代验算。 |
| **AI Agent 开发者** | 给 Agent 一个**确定性数学后端**：同样的约束每次返回完全一致、可验证的实数解，推理才 reproducible。这是大模型原生做不到的。 |
| 企业 / 私有化 | 确定性数值求解，数据不出域、可自托管、已验证解数学保真，适合嵌入内部系统或 Agent 管线。 |

## 8. 是否免费

- **网页版**：免费，打开即用。
- **MCP（clone 自托管）**：免费，零依赖 `node mcp-server.js`。
- **远程端点（beta）**：当前免费试用。
- **企业自托管 / 云端按量**：规划中的收费线（卖点 = 确定性 + 数据不出域 + 高并发）。

## 9. 安全与隐私

- **网页版**：纯前端，计算在浏览器内完成，**数据不出本机**。
- **MCP 自托管**：部署在用户自己的环境，**数据不出域**。
- **远程端点**：方程发往服务端计算；调用日志**仅记元数据（不记方程内容）**，结构化错误不泄露堆栈。
- **零依赖**：纯 TypeScript/Node 内置模块，无第三方运行时供应链风险。

## 10. 其他要考虑的

- **确定性**：同输入同输出，可安全缓存与重复调用。
- **不是高精度全域求解器**：6 位小数有限网格，面向个人/会计/AI Agent 的轻量数值工具，非科学计算级全域精度。
- **不支持项（诚实标注）**：整数强制约束、必不等于约束目前做不了。
- **性能预算**：全局分支定界受 `budget`/`maxDepth` 限制；超预算标 `truncated`，可加预算或缩域重试。

---

## 11. 大模型 / 智能体调用时会看什么（重点）

**Agent 不会读落地页，它只读 MCP 的 `tools/list` 返回：** 工具 `name` + `description` + `inputSchema`（有的客户端还会读示例）。

- `description` 就是它决定"**该不该调、怎么调、结果怎么读**"的唯一说明书。
- 所以**工具描述必须详细、诚实、含使用契约**——这比任何对外文案都重要。

### 11.1 优化后的 `solve` 工具描述（已写入 mcp-server.js / http-mcp-server.js / smithery-listing.md）

```
求解实数方程组的确定性数值引擎（非大模型，无随机、同输入输出可复现）。
适用：需可验证、可复现的实数解（代数或 sin/cos/tan/log/exp/sqrt/abs 等常见超越函数），尤其给 AI Agent 当"不会胡说"的数学后端。
不适用：纯符号推导/闭式证明、微分方程初值问题、整数/必不等于等强制约束（暂不支持）。

输入：equations 为含 "=" 的方程字符串数组，如 ["x^2+y^2=25","x+y=7"]；variables 可选（不填自动识别，最多6个）；domain 可选（如 {"x":[-30,30]}），否则默认每变量 ±1e6。
硬限制：变量 ≤6；方程 1–64 条且数量须 ≥ 变量数；单次方程文本 ≤100KB；输出固定 6 位小数（不可切换）。

输出（结构化 JSON）：
- resultType：empty(经 sound 算子严格证无实数解) / finite(有限个已验证解) / infinite(无限解集，仅给距原点最近的推荐解)。
- solutions[]：每解含 values[](数值解) 与 tier(proven=Krawczyk 已认证 / likely / candidate) 及 residual(残差)。
- certified：是否全部 proven；recommended：范数最小（距原点最近）的解。
- truncated=true：预算内未完成全局分支判定、未证明已穷尽——不等于一定漏解，多数情况全部真解已找到；极端病态下可能遗漏个别解，可缩 domain 或提高 budget 重试。

错误：返回 error.type（invalid_input=输入不合法/超限，internal_error=内部异常）。遇卡点或认为结果有误，请调用 give_feedback（内容仅落本地日志，不外传）。
确定性：相同输入永远返回完全相同结果，可安全缓存与重复调用。
```

### 11.2 Agent 解析契约（给接入方）

| 想判断 | 看哪个字段 |
|---|---|
| 有没有解 | `resultType`（`empty`/`finite`/`infinite`） |
| 解可不可信 | `certified`（全 proven）与每解 `tier` |
| 用哪个解 | `recommended`（距原点最近） |
| 是否可能漏解 | `truncated`（true=未证明穷尽，需重试/缩域） |
| 出错没 | 响应 `isError` + `error.type` |

---

## 12. 如何最大化让别人选 + 用得方便 + 反馈方便

- **降低门槛**：网页版零安装；MCP 一行 `node mcp-server.js` 即起；提供复制即用的客户端配置块。
- **诚实即信任**：明确 `truncated`、明确不支持项，反而提高专业可信度，促选用。
- **反馈闭环**：内置 `give_feedback` 工具（Agent 可主动回报）；网页版/仓库提供 GitHub Issues 入口；反馈内容不外传，降低用户顾虑。
- **首屏短、规格全**：人看的首屏只讲"确定性"一句话；所有细节放在本"完整版"规格页，需要深度的人自己查。

## 13. 对外口径红线（诚实，不可越）

- ✅ 说：确定性、可验证、无幻觉、同输入同输出、已验证解数学保真、全局穷尽尽力而为、最多6变量、方程≥变量数（≤64护栏）、范围±100万、6位小数。
- ❌ 不说："保证不漏解""100% 全覆盖""绝对精确"。
- ❌ 不再提"10方程限制"（旧叙事，当前方程上限为 64 护栏，无"10"封顶）。
- ❌ 不编功能：整数强制 / 必不等于约束目前做不了。
