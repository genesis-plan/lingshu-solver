# 更新日志（Changelog）

> 只记录**对用户重要的变更**（能力、边界、修复）。内部开发过程不在此记录。
> 当前发布版本：**`lingshu-solver@1.0.6`**（npm，2026-09-04）；含修2（两处 P0）+ 修3（solve 超越单变量挂起）。

## 1.0.6（npm 发版，2026-09-04）
- **修3 · 顶层 solve 对单变量超越方程挂起（#P0-3，超时/可用性）**：单变量 `sin/cos/exp/tan` 等周期方程在默认 ±1e6 声明域上根数达数十万，`suan22` 网格扫描 + 二分定位对每个异号区间调用 `intervalEval`/牛顿，全部挤在**一次 `_runOp` 调用内**，全局 8s 看门狗在算子返回前无法触发，致 `solve('sin(x)-0.5')` 挂起 >110s。修复：在扫描循环加**根数护栏（>200）+ 时间护栏（>1.5s）**提前终止；根数超阈值时——
  - 含周期函数（已检出周期）⇒ 解集实际为**无限**（周期平移），如实标 `resultType=3 无限解集(代表解)`，给出部分代表根并提示收窄定义域；
  - 非周期但稠密（如高次多项式）⇒ **不谎称无限**，置 `truncated` 诚实截断并告警，返回部分代表根。
  - 多项式与多变量方阵 `solve`、限域周期方程（如 `sin(x)=0.5, x∈[0,4]`）行为不变（proven / 有限集，毫秒级）。
  - 验证：`solve('sin(x)-0.5')` 现 **666ms 返回 rt=3**（旧 >110s 挂起）；`solve('tan(x)=1')` 615ms rt=3；限域 `cos(x)=0.5, x∈[0,20]` 10ms 返回 7 个 proved 根。
- **回归测试扩展** `test/p0_fix_regression.js`：共 18 项全绿，新增 F 节覆盖修3 五类超越单变量用例（周期无限/限域有限/无根），`npm test` 主验证。

## 1.0.5（npm 发版，2026-09-04）
- **P0 修复 · 全局分支定界谎报 complete（#P0-1）**：`_newtonRefine` 把裸**点值**（number）喂给 `intervalEval` 求雅可比，而 `intervalEval` 期望 `{min,max}` 区间——裸数的 `iv.min/iv.max` 为 `undefined` → `_buildAffEnv` 算出 `NaN` 噪声 → 雅可比整行 `NaN` → `realMatInv` 失败 → 对一切良置问题**恒返 `null`**。后果：`_globalBranchCertify` 的 Krawczyk 收敛盒中点永远无法精化为真解，却仍把该盒消费掉，最终对 `x^2-2` 之类方程返回 **0 解却 `complete=true`**（假证"已穷尽无遗漏"）。修复：雅可比求导改用退化点区间 `{min:x,max:x}`，使 `intervalEval` 正常求值。验证：`_newtonRefine(x^2-2, 1.5) → √2`；`_globalBranchCertify(x^2-2)@[-3,3]` 现返回 **2 解且 `complete=true`**。
- **P0 修复 · 超越函数区间塌缩为点（#P0-2，soundness 破坏）**：`_affineEval` 的 `func` 分支算得正确区间 `rv` 后却 `return _Aff((rv.min+rv.max)/2, {})`——把合法区间宽度丢成 0，致 `intervalEval(sin x)@[-2,2]` 塌成 `[0,0]`、`d/dx[sin(x)]` 雅可比退化为中点点值。后果：对含 sin/cos/exp/ln/sqrt 等超越函数的方程，区间包络与 Krawczyk 雅可比**不可信**（声称的包含区间实际不含真值）。修复：用半径 `(rv.max-rv.min)/2` 的噪声符号把 `rv` 完整编码为仿射，使 `_affToInterval` 还原为 `[rv.min, rv.max]`（sound 且紧致）。验证：`intervalEval(sin x)@[-2,2] → [-1,1]`；`d/dx[sin(x)-0.5]@[-2,2] → [-cos2, 1]`。
- **新增回归测试** `test/p0_fix_regression.js`（13 项全绿）：覆盖上述两 P0 的修复前/后行为，作为 `npm test` 主验证。
- **已知未修（独立既有缺陷，非本版引入）**：顶层 `solve()` 对单变量超越方程（如 `sin(x)-0.5`）挂起问题**已在 1.0.6（修3）修复**：扫描循环加根数/时间护栏，周期稠密解集如实标无限解集(代表解)。

## 1.0.4（npm 发版，2026-09-03）
- **许可证变更（重大）**：自本版本起，由 Apache License 2.0 变更为**自有《灵数求解器商业授权许可协议》**——非商业用途免费（个人学习/研究/教学/评测、非营利与教育机构内部、小团队评估 ≤3 实例），**商业用途须事先取得版权方书面授权**（联系 553420544@qq.com）。
- **版本适用边界（诚实条款）**：1.0.3 及更早版本按其发布时随附的 Apache License 2.0 提供，该授权对已发布版本不可撤回；但 Apache 授权不延伸适用于 1.0.4 起的任何新版本。
- 本版本除许可证与元数据（package.json / glama.json / README 许可节）外，**求解核心代码与 1.0.3 完全一致**，无任何算法或行为变更。

## 1.0.3（npm 发版，2026-09-01）
本版本将以下已上线的核心修复与安全加固带入 npm 包（自 1.0.2 以来累计）：
- **解析修复**：`log2`/`log10` 不再被误线性化；豆包等 AI 粘贴的 Unicode 符号（`−`/`·`/`≠`）与竖排换行输入可正常解析；希腊变量名（theta/alpha 等）的 `domain` 键同步映射，不再被静默丢弃。
- **issue A 修复**：单变量周期/高频方程（`cos(x)=0.5` 等）多解不再静默漏报。
- **P0 修复**：只给部分变量 `domain` 不再崩溃。
- **合规门禁**：`solve` 总入口拒收非数学自然语言文本、100KB 体量上限；手机号/证件号等数字按数学常量正常参与计算。
- **MCP HTTP 服务端加固**：请求体 256KB 上限、每 IP 120 次/分限速、会话 TTL 清理、日志轮转、反馈落盘脱敏。
- **新增**：`docs/ARCHITECTURE.md` 产品现状文档；`/privacy.html` 隐私与安全说明页；`docs/OPERATORS.md` 算子手册（suan1–suan49 全表 + 分层说明）；`docs/RETROSPECTIVE.md` 项目多日完整复盘（全景/时间线/工作流决策/战略转向/工程+认知坑/真实状态/有意不做）。

## 2026-09-01（安全等保加固，npm 版本号未变）
- **仓库文档整备**：清理过程性/内部文档（推广工具包、话术、曝光文案、内部走查与审计报告、Smithery 提交记录、roadmap、调试脚本），新增 **`docs/ARCHITECTURE.md` 产品现状文档**——数学框架（区间算术/六大收缩算子/Krawczyk 认证/分支定界/诚实边界）、结构框架、代码框架、算法框架、端到端处理流程，忠实于真实实现。
- **合规输入门禁（网页端与 MCP 端共用同一入口）**：本工具是纯数学方程求解器，现已在 `solve` 总入口统一拦截——① 自然语言文字（中/日/韩）；② 方程文本总长 100KB 上限与 MCP 端护栏对齐。合法数学表达零误伤（回归 28 用例 0 崩溃、known 94% 无回退）。
- **输入放宽（同日晚）**：手机号 / 身份证号 / 银行卡号等长数字串与邮箱**不再拦截**，一律按普通数学常量放行参与计算——它们是合法数学输入，本系统本就不存储、不外传。仅保留"非数学文字拒收 + 100KB 体量上限"两条与求解本身相关的护栏。
- **XSS 加固（网页）**：结果区 `innerHTML` 的所有用户可控内容（变量名、诊断信息、警告等）一律 HTML 转义。
- **MCP HTTP 服务端防攻击加固**：① 请求体上限 256KB（超限 413，防内存耗尽）；② 每 IP 限速 120 次/分钟（超限 429，可用环境变量覆盖）；③ 会话表 TTL 自动清理（防无限增长）；④ 日志超 5MB 自动轮转；⑤ `give_feedback` 反馈落盘前自动隐去手机号/证件号/邮箱/超长数字串。
- **安全响应头（nginx）**：Content-Security-Policy、X-Frame-Options: DENY（禁止被第三方页面嵌入）、X-Content-Type-Options、Referrer-Policy、Permissions-Policy（禁用摄像头/麦克风/定位）、server_tokens off。
- **新增《隐私与安全说明》页**：`/privacy.html`，首页页脚可进入。核心承诺：网页端方程在浏览器本地计算、不上传；不收集任何个人信息；无图片/文件上传；结果仅请求者本人可见；无评论/分享等公开 UGC 功能（不具舆论属性）；无 Cookie/埋点/广告。
## 2026-09-01（求解核心修复，npm 版本号未变）
- **修复 · 传 `domain` 时崩溃（P0）**：用户只给**部分变量**的搜索域（如 `{"x":[0,10]}` 而方程含 x/y）会直接抛异常。根因两处——① D0 初始化对未指定变量 `continue` 跳过、致其无 `{min,max}` 条目；② 全局分支定界 `_globalBranchCertify` 内部按 `dom[v][0]/[1]` 取数组格式域，直接传入原始 `userDomain` 时缺失变量为 `undefined` → `undefined[0]` 崩溃。现为缺失变量回退初始域快照（仍缺失则用默认 ±1e6）并统一为数组格式。
- **修正文档不实数据**：README / CHANGELOG / roadmap 原称回归「known 命中 50/50（100%）」，实测为 **47/50（94%）**（`mcp.json` 考卷 3 个纯算术求值型用例未命中，见下）。已如实更正。
- **修复 · issue A：周期/高频单变量方程静默漏支（假阴性）**：`cos(x)=0.5`、`sin(x)=0.5`、`sin(100x)=0`、`tan(x)=1` 等单变量周期/高频方程，此前经"阶段 3.5 提前多起点牛顿"短路只返回 1 个基解且不标 `truncated`，对多周期输入漏报其余真解（如 `cos(x)=0.5` 在 `[0,2π]` 只给 1 解、`[0,20]` 给 4 解；`sin(100x)=0` 在 `[0,1]` 只给 2 解）。现仅对"纯净单变量输入"（原始即 1 方程 1 变量、且方程仅引用该变量）清空短路标志，交 `suan22` 周期感知自适应扫描补全全部根；多变量方阵、以及消元后"伪单变量"（含自由参数，如 M01/T_Wikibooks）保持原短路行为以保留自由参数采样得到的多解。**验证**：本地回归 28 用例 0 崩溃、known 47/50（94%）无回退；15 例周期/高频专项（含 `sin(100x)=0`→32、`cos(x)=0.5 [0,20]`→7）全过；远程 MCP 端点实测一致。
- **修复 · `log2`/`log10` 被错误线性化（correctness bug，影响第6题这类对数方程）**：`log2(x)`、`log10(x)` 此前被 `fuzzyFix` 的"函数名补括号"正则误拆为 `log(2)*x` / `log(10)*x`，导致 `log2(x)=1` 解出 `x≈3.321928`（应为 2）、`log2(x-1)+log2(x+2)=2` 解出 `x≈2.821928`（应为 2）等**错误答案**且仍标 proven。根因：函数名 alternation 中裸 `log` 排在 `log2`/`log10` 之后，正则回溯让 `log` 抢走 `2`/`10` 作捕获组首字符。已对裸 `log` 加负向先行 `(?!2|10)`，使 `log2`/`log10` 完整匹配不再被拆。验证：`log2(x)=1`→2、`log2(x)=2`→4、`log2(x-1)+log2(x+2)=2`→2 等 6 例全过。
- **修复 · 从豆包/通义等 AI 复制的方程解析失败（变量看似"未识别"）**：核心/MCP 路径只走 `fuzzyFix`、不走 web 的 `cleanInput`，而 `fuzzyFix` 此前未归一化 Unicode 数学符号 `−`（U+2212 减号）、`·`（U+00B7 中点）、`≠`（U+2260 不等号）。这些符号随豆包复制粘贴带入，lexer 抛"无法识别的字符"→ 解析失败 → `solve` 返回空变量、0 解，用户误以为"变量没自动识别"。已在 `fuzzyFix` 补齐：−/–/—→`-`、·→`*`、≠→`!=`。（`×`/`÷` 此前已处理；`²`/`³` 由上标规则转 `^N`；`√` 转 `sqrt(`。）现 web 与 MCP 两条路径对豆包粘贴的 `−`/`·` 均能正常识别变量并求解。
- **修复 · 豆包式"打竖"粘贴（换行打断隐式乘法）**：豆包等 AI 渲染数学时每个字符常独立成行，复制后带入换行。**换行会打断隐式乘法的相邻性**——`4⏎(x-2)`、`2⏎x` 中的换行使 `(\d)\(`→`*`、`(\d)[a-z]`→`*` 等规则失效，即便 U+2212 已归一化仍 0 解（此前误判"换行被 lexer 忽略"，实为错判）。现双层修复：① 核心路径 `fuzzyFix` 直接去除单条方程内的换行（方程以"数组"为契约，不影响数组层面的多方程分隔）；② web 文本框在"整段仅含一个 `=` 却跨多行"时判定为单条方程被拆散、自动合并换行（多行方程组每行各含 `=` 不受影响）。验证：竖排 `4(x−2)+3=2x+7`→`x=6`、竖排 `2·x=6`→`x=3`，远程 MCP 实测一致。
- **修复 · 希腊变量名的域键失配（theta/alpha 等被归一化为 θ/α 后用户域被静默丢弃）**：`solve` 在归一化阶段把 `theta`/`alpha` 等拼写统一映射为希腊符号（θ/α），varNames 与方程文本都映射了，但用户传入的 `domain` 对象**键未同步映射**——`{theta:[0,2π]}` 与归一化后的变量 `θ` 失配 → 域被静默丢弃 → 退回默认 ±1e6。对周期方程（如 `sin(theta)=0.5`）意味着在全域穷举数十万根，表现为长时间无响应。现已把 `domain` 键按同一规则映射（含声明 `e`→`ₑ` 的保护）。验证：`sin(theta)=0.5` 配 `{theta:[0,2π]}` 显式变量与自动识别两路径均 30ms 内返回 2 根，远程 MCP 实测一致；回归 47/50（94%）无回退。
- **性能提示（已知行为，非缺陷）**：单变量周期方程在**默认全域 ±1e6** 下真根数量巨大（如 `sin(x)=0.5` 约 63 万根），修复 issue A 后会诚实穷举，耗时可达分钟级。如只需局部根，请显式传入较小的 `domain`（如 `[0, 2π]`）。
- **保持不修 · issue B（双曲/反三角）**：`sinh/cosh/tanh/asin/acos/atan` 仍不被识别（被词法拆成变量乘积→误报无解）。按产品决策**有意不补**（低频、易与隐式乘规则冲突），继续列为已知限制，不在本次加入一元函数表。
- 已知缺陷（未修，诚实列出）：给**窄**域反而可能判无解（如 `x1=12, x2=5, x1/x2-x3=0` 配 `x3:[2,3]` → 0 解，配 `x3:[0,10]` → 正常解出 2.4）。仅影响含纯常量等式的退化算术题，常规方程（含网页 6 个示例）不受影响。

## 2026-08-31（仓库与官网，npm 版本号未变）
- **官网与远程端点开放**：域名 ICP 备案已通过，`hongchenlingjing.com` 正式上线——首页产品介绍、在线求解器（`/index.html`）、远程 MCP 端点（`/mcp`，HTTPS，Streamable HTTP）、健康检查（`/health`）。
- **C1 · 整数约束不再静默实数化**：用户写 `x∈ℤ` 时，输出新增结构化标志 `integerConstraintUnenforced`（与 `truncated` 同级，MCP 自动透传），并如实提示"返回解不一定为整数"。此前只在 `warnings` 里放一段文字，程序化调用方无法检测。
- **A1 · 一阶仿射算术**：`intervalEval` 内部改为仿射形式求值后转回保守区间，缓解区间算术的"相依性灾难"（如 `x-x` 虚假膨胀）。Krawczyk 仍消费保守区间，soundness 不变。
- **D1 · 认证半径取最紧**：`krawczykCertify` 注释宣称"取最小成功半径"、实现却取第一个成功（候选半径顺序非单调）。已改为遍历取最小，兑现 README 承诺的"误差 ≤ 认证半径"。
- **修复**：仓库部分 Markdown 文件曾被误存为 base64 文本（GitHub 上显示为一整行乱码），已全部还原为原文。

## v1.0.2
- 求解核心稳定性：区间分支定界 + Krawczyk 认证路径加固，回归套件 28 用例、0 崩溃，known 命中 47/50（94%；未命中的 3 例为纯算术求值型题目，非方程求解主场景）。
- 诚实边界显式化：极端病态输入在预算内可能标记为 `truncated=true`，绝不谎称已穷尽。
- 远程 HTTP 服务端 `http-mcp-server.js` 与 stdio 端 `mcp-server.js` 共享同一求解核心，结果同源一致。

## v1.0.0 — 首个公开版
- 确定性实数方程组求解引擎（≤6 变量、实数解、轻量数值定位），不要求初值。
- 三种接入形态：本地 stdio（`npx -y lingshu-solver`）、本地 clone、远程 HTTP（备案期间暂停，2026-08-31 起开放）。
- MCP 工具：`solve`（Krawczyk 认证解，6 位小数精度）、`give_feedback`（本地落日志，不外传）。
- 纯本地、离线、零数据；无随机分支，同输入永远同输出。
- 网页版 `index.html` 单文件产品（浏览器内 UI + 已验证核心），GitHub Pages 与腾讯云 COS 双托管。

## 能力与边界（恒定）
- 已验证解：每解经 Krawczyk 认证（`tier=proven`），误差 ≤ 认证半径，数学保真。
- 穷尽性：**尽力穷尽多解**；无法证明已穷尽时显式 `truncated`，非缺陷。
- 数值范围：默认搜索域 ±1e6；快增长函数（`exp`/`sinh`）或大域建议显式给定 `domain`。
- 不保证：对一切输入 100% 穷尽；高度病态系统在预算内必收敛。
