# 灵数求解器 · 对外曝光文案（复制即用）

> 统一口径：**确定性**（非大模型、不随机、不幻觉、每个解可验证）。免费、网页打开即用、也能被 AI 智能体当工具调用。
> 链接：`https://genesis-plan.github.io/lingshu-solver/` ｜ 介绍页：`https://genesis-plan.github.io/lingshu-solver/intro.html`

---

## 一、中文社区（复制即发）

### 1. 知乎（回答 / 文章）
**标题**：有没有「不会胡说八道」的方程求解工具？
**正文**：
> 市面上让 AI 算方程，大多是大模型在「猜」，看着像那么回事，其实错得离谱还没法验证。
> 我做了一个确定性的：同样一道题，永远得到同样的答案；每个解都会当场验证对错，不会编。
> 最多 6 个未知数、范围 ±100 万、保留 6 位小数，网页打开就能用，不用装任何东西，免费。
> 也能作为 MCP 工具给 Claude / Cursor 这类 AI 助手调用，让它们算数时别再瞎编。
> 地址：https://genesis-plan.github.io/lingshu-solver/

### 2. CSDN（博客）
**标题**：开源确定性方程求解器：让 AI 算数不再「一本正经地胡说」
**正文**：
> 在大模型时代，数学计算反而更不可信了——LLM 会幻觉。
> 灵数求解器走另一条路：**完全确定性**，无随机、无模型、无 API。
> - 网页直接算，离线运行，数据不出浏览器
> - 输出全部实数解，每个解带验证与残差
> - 同时提供 MCP 接口，AI 智能体可一键调用
> - 免费、零依赖、纯前端
> 在线体验：https://genesis-plan.github.io/lingshu-solver/

### 3. 掘金（文章）
**标题**：给 AI Agent 一个「不会算错」的算数工具：灵数求解器
**正文**：
> Agent 调用大模型算 `x^2 + y^2 = 25, x + y = 7` 这类方程组，经常给出不验证的近似值。
> 灵数求解器把这件事做「确定」：同输入必得同输出，解全部可回代验证。
> 支持浏览器直用 + MCP 远程调用，适合接进你自己的 Agent 工作流。免费开源。
> 试一下：https://genesis-plan.github.io/lingshu-solver/

### 4. 微信公众号（推文短版）
> 算方程总被 AI 瞎编？试试灵数求解器：确定性算法，结果可验证、不随机、不幻觉。
> 网页打开即用，免费，也能让 AI 助手直接调用。👉 https://genesis-plan.github.io/lingshu-solver/

---

## 二、海外社区（复制即发）

### 5. Hacker News
**Title**: Show HN: Lingshu Solver – a deterministic equation solver (no LLM, verifiable)
**Text**:
> Most "AI math" tools just have an LLM guess and confidently hallucinate. Lingshu Solver is deterministic: same input always yields the same output, and every real solution is verified and mathematically faithful. Runs in the browser (no install) or as an MCP tool for agents. Free, zero dependencies.
> Demo: https://genesis-plan.github.io/lingshu-solver/

### 6. Reddit – r/mcp
**Title**: Lingshu Solver: a deterministic (non-LLM) equation-system solver with an MCP endpoint
**Body**:
> I built a deterministic algebraic equation-system solver that AI agents (and people) can use. No LLM inside, so no randomness or hallucination — same input gives the same verified output. It exposes an MCP tool over HTTP and also runs in the browser. Free, zero deps. Looking for feedback from the MCP community.
> https://genesis-plan.github.io/lingshu-solver/

### 7. Reddit – r/LocalLLaMA
**Title**: Stop letting your LLM guess math: a deterministic solver you can wire into agents
**Body**:
> If your agent needs to actually solve equations (not approximate them), a deterministic solver helps. Lingshu Solver returns all real solutions, each verified; usable via browser or MCP. Free.
> https://genesis-plan.github.io/lingshu-solver/

### 8. Product Hunt（文案草稿）
**Tagline**: A deterministic equation solver that never hallucinates
**Description**:
> Lingshu Solver solves systems of real equations with verified, mathematically faithful results — no LLM, no randomness, no API. Use it in the browser or as an MCP tool for your AI agents. Free and open.
> Link: https://genesis-plan.github.io/lingshu-solver/

---

## 三、一句话电梯陈述（任意场景）
> 灵数求解器：一个确定性的方程求解工具，答案永远准确、每个解都能验证，免费、打开网页就能用，也能让 AI 智能体直接调用。
